- name: Configure worker servers
  hosts: workers
  become: true

  tasks:
    - name: Insure Curl is install
      ansible.builtin.apt:
        name: curl
        state: present
    - name: Create Ansilbe file
      ansible.builtin.file:
        path: /opt/ansible-test.txt
        state: touch

    




